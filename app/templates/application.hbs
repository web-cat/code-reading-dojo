{{!-- app/templates/application.hbs --}}
<!-- {{#if session.isAuthenticated}}
  <button {{action 'invalidateSession'}} class="btn btn-primary btn-lg" id="logout">logout</button>

{{else}}
  {{#link-to 'login' class="btn btn-primary btn-lg" id="login"}}
    login
  {{/link-to}}
{{/if}}
{{outlet}} -->

<div class="menu">
  {{#if session.isAuthenticated}}
    <a class="btn btn-primary btn-lg" id="logout" {{action 'invalidateSession'}}>Logout</a>
  {{else}}
    {{#link-to 'login' class="btn btn-primary btn-lg" id="login"}}Login{{/link-to}}
  {{/if}}
</div>
<div class="main">
  {{outlet}}
</div>
